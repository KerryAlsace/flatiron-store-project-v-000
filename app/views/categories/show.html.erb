<h1><%= @category.title %></h1>

<ul>
<% if user_signed_in? %>
  <% @category.items.each do |item| %>
    <li><%= item.title %> | <%= "$#{item.price.to_f/100}" %> | <%= button_to 'Add to Cart', cart_path(current_user.current_cart) %></li>
  <% end %>
<% else %>
  <% @category.items.each do |item| %>
    <li><%= item.title %> | <%= "$#{item.price.to_f/100}" %></li>
  <% end %>
<% end %>
</ul>