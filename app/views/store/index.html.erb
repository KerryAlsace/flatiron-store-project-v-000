<% @categories.each do |category| %>
  <%= link_to category.title, category_path(category) %>
<% end %>


<% if user_signed_in? %>
  <% @items.each do |item| %>
    <p><%= item.title %> | 
    <%= "$#{item.price.to_f/100}" %> | 
    <%= button_to 'Add to Cart', cart_path(@current_cart), params: {item_id: item.id} %></p>
  <% end %>
<% else %>
  <% @items.each do |item| %>
    <p><%= item.title %> | <%= "$#{item.price.to_f/100}" %></p>
  <% end %>
<% end %>